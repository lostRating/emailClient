使用python的socket库提供的连接、发送、接收功能

对于smtp的实现
先socket连接，然后按照握手、登录请求、加密的用户名、加密的密码、发件人、收件人、邮件内容这样的顺序与服务器交互，根据返回的code判断是否出问题

对于pop3的实现
socket先连接，然后发送用户名、密码成功后，则可用其提供的一些功能，如查询邮件总数、下载某一个邮件等，返回的并不是code而是ok或者err